# 🚀 Web App Hosting Dashboard

Git 레포지토리를 활용한 정적 HTML 웹 앱 호스팅 및 관리 시스템입니다. 대시보드에서 모든 앱을 한곳에서 관리, 검색, 정렬할 수 있습니다.

## 📋 목차

- [주요 기능](#주요-기능)
- [빠른 시작](#빠른-시작)
- [폴더 구조](#폴더-구조)
- [사용 방법](#사용-방법)
- [앱 추가 방법](#앱-추가-방법)
- [GitHub Actions 자동화](#github-actions-자동화)
- [문제 해결](#문제-해결)

---

## 🎯 주요 기능

### 1. 🔍 검색 기능
- 앱 이름, 설명, 태그, 기능으로 검색
- 엔터 키 또는 검색 버튼으로 실행
- 실시간 검색 결과 표시

### 2. 📁 폴더트리 보기
- Git 폴더 구조 시각화
- 재귀적 폴더 트리 표시
- 폴더 클릭으로 앱 접속

### 3. 📋 모든 앱 보기
- 카드형 UI로 모든 앱 표시
- 이름 오름차순/내림차순 정렬
- 아이콘, 설명, 태그, 기능 표시

### 4. 📊 통계 탭
- 전체 앱 수
- 마지막 업데이트 시간 (년월일시분초)
- 태그별 앱 개수

### 5. ⚙️ 자동 메타데이터 관리
- Push마다 자동으로 폴더트리 생성
- 모든 앱의 메타데이터 자동 수집
- 통계 데이터 자동 생성
- GitHub Actions로 완전 자동화

---

## ⚡ 빠른 시작

### Step 1: 레포지토리 복제 및 폴더 구조 생성

```bash
# 레포지토리 복제
git clone https://your-repo-url
cd your-repo

# 필요한 폴더 생성
mkdir -p apps _data _config assets/{icons,styles,scripts}
mkdir -p .github/workflows
```

### Step 2: 필수 파일 생성

#### 2-1. `.github/workflows/generate-metadata.yml`
제공된 `github-actions-workflow.yml` 파일을 복사하여 `.github/workflows/generate-metadata.yml`로 저장합니다.

#### 2-2. `index.html`
제공된 `dashboard-index.html` 파일을 복사하여 `index.html`로 저장합니다.

#### 2-3. `.gitignore`
제공된 `gitignore-file` 파일을 복사하여 `.gitignore`로 저장합니다.

#### 2-4. `_config/web-app-template.md`
제공된 `web-app-template.md` 파일을 `_config/` 폴더에 복사합니다.

### Step 3: 첫 번째 커밋 및 푸시

```bash
git add .
git commit -m "Initial setup: dashboard and workflow"
git push origin main
```

### Step 4: GitHub Actions 확인

1. GitHub 레포지토리 페이지로 이동
2. "Actions" 탭 클릭
3. "Generate Metadata" 워크플로우 실행 확인
4. 완료 후 `_data/metadata.json`, `_data/tree.json`, `_data/stats.json` 자동 생성 확인

### Step 5: 대시보드 접속

```
https://username.github.io/repo-name/
```

---

## 📁 폴더 구조

```
repository-root/
├── index.html                          # 대시보드 메인 페이지
├── .github/
│   └── workflows/
│       └── generate-metadata.yml       # 메타데이터 자동 생성 워크플로우
├── _data/
│   ├── tree.json                       # [자동 생성] 폴더트리 구조
│   ├── metadata.json                   # [자동 생성] 모든 앱의 메타데이터
│   └── stats.json                      # [자동 생성] 통계 데이터
├── _config/
│   └── web-app-template.md             # 웹 앱 제작 요청서 템플릿
├── apps/
│   ├── calculator/
│   │   ├── index.html                  # 앱 메인 HTML
│   │   ├── metadata.json               # 앱 정보 (필수)
│   │   ├── style.css (선택)
│   │   └── script.js (선택)
│   ├── todo-list/
│   │   ├── index.html
│   │   └── metadata.json
│   └── [더 많은 앱들...]
├── assets/
│   ├── icons/
│   │   ├── calculator.svg
│   │   ├── todo-list.svg
│   │   └── [앱 아이콘들...]
│   ├── styles/
│   │   └── [공유 스타일 시트 - 선택]
│   └── scripts/
│       └── [공유 스크립트 - 선택]
├── .github/
│   └── workflows/
│       └── generate-metadata.yml
├── .gitignore
└── README.md
```

---

## 📖 사용 방법

### 대시보드 사용법

#### 🔍 검색 탭
```
1. 검색창에 원하는 단어 입력
2. Enter 키 누르거나 [검색] 버튼 클릭
3. 검색 범위: 앱 이름, 설명, 태그, 기능
4. 결과 카드를 클릭하여 앱 열기
```

#### 📁 폴더트리 탭
```
1. 폴더 아이콘 클릭으로 펼치기/접기
2. 파일 클릭하여 해당 리소스 열기 (HTML만 실행 가능)
3. 전체 레포지토리 구조 시각화
```

#### 📋 모든 앱 탭
```
1. [정렬] 드롭다운으로 정렬 방식 선택
   - 이름 오름차순 (A→Z)
   - 이름 내림차순 (Z→A)
2. 앱 카드 클릭하여 앱 열기
3. 각 카드에서 앱 정보 확인:
   - 아이콘, 이름, 설명
   - 태그 (분류)
   - 주요 기능
```

#### 📊 통계 탭
```
1. 전체 앱 개수 확인
2. 마지막 업데이트 시간 확인 (년-월-일 시:분:초)
3. 태그별 앱 개수 통계 확인
```

---

## 🎨 앱 추가 방법

### 기본 3단계 프로세스

#### Step 1: 폴더 및 파일 생성

```bash
# 새 앱 폴더 생성 (app-id는 영문 소문자 + 하이픈)
mkdir -p apps/my-app

# 메인 HTML 파일 생성
cat > apps/my-app/index.html << 'EOF'
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My App</title>
    <style>
        /* 스타일 임베드 */
    </style>
</head>
<body>
    <!-- 앱 콘텐츠 -->
    <script>
        // 스크립트 임베드
    </script>
</body>
</html>
EOF
```

#### Step 2: 메타데이터 파일 생성

```bash
cat > apps/my-app/metadata.json << 'EOF'
{
  "id": "my-app",
  "name": "내 앱",
  "description": "간단한 앱 설명",
  "icon": "../../assets/icons/my-app.svg",
  "tags": ["유틸리티", "예시"],
  "features": ["기능1", "기능2"],
  "version": "1.0.0",
  "author": "Developer Name",
  "createdAt": "2025-01-15T10:30:00Z",
  "updatedAt": "2025-01-15T10:30:00Z"
}
EOF
```

#### Step 3: Git에 추가 및 푸시

```bash
git add apps/my-app/
git commit -m "Add my-app"
git push origin main
```

→ GitHub Actions 자동 실행 → 메타데이터 자동 생성 → 대시보드에 반영

### 메타데이터 필드 설명

| 필드 | 설명 | 예시 |
|-----|------|------|
| `id` | 앱 고유 ID (폴더명과 동일) | weather-app |
| `name` | 대시보드에 표시될 앱 이름 | 날씨 앱 |
| `description` | 앱 한두 줄 설명 | 실시간 날씨를 조회합니다 |
| `icon` | 아이콘 SVG 경로 | ../../assets/icons/weather-app.svg |
| `tags` | 분류 태그 배열 | ["유틸리티", "날씨"] |
| `features` | 주요 기능 배열 | ["검색", "예보", "알림"] |
| `version` | 시맨틱 버전 | 1.0.0 |
| `author` | 개발자 이름 | AI Generated |
| `createdAt` | 생성 시간 (ISO 8601) | 2025-01-15T10:30:00Z |
| `updatedAt` | 수정 시간 (ISO 8601) | 2025-01-15T10:30:00Z |

### 앱 개발 체크리스트

- [ ] 영문 소문자 + 하이픈으로 폴더명 설정
- [ ] `index.html` 완전한 HTML5 구조
- [ ] 모든 CSS/JS 임베드 (외부 파일 참조 최소화)
- [ ] `metadata.json` 정확한 정보 입력
- [ ] (선택) 100x100px SVG 아이콘 생성
- [ ] 로컬에서 앱 동작 테스트
- [ ] 메타데이터 JSON 형식 검증
- [ ] 모바일 반응형 확인
- [ ] Git push 후 Actions 실행 확인
- [ ] 대시보드에서 앱 표시 확인

---

## 🤖 GitHub Actions 자동화

### 워크플로우 개요

```
┌─ Push to main branch
│
├─ Checkout code
├─ Setup Node.js
├─ Generate metadata.js 생성
├─ 스크립트 실행
│  ├─ apps/ 폴더 스캔
│  ├─ 각 앱의 metadata.json 읽기
│  ├─ 폴더트리 구조 분석
│  └─ _data/ 디렉토리에 파일 생성
│     ├─ tree.json (폴더 구조)
│     ├─ metadata.json (모든 앱 정보)
│     └─ stats.json (통계)
└─ Git commit & push
```

### 생성되는 파일

#### `_data/metadata.json`
```json
{
  "generatedAt": "2025-01-15T10:30:45Z",
  "totalApps": 3,
  "lastUpdated": "2025-01-15 10:30:45",
  "apps": [
    {
      "id": "calculator",
      "name": "계산기",
      "path": "apps/calculator/index.html",
      "description": "기본 계산기",
      "icon": "../../assets/icons/calculator.svg",
      "tags": ["유틸리티"],
      "features": ["덧셈", "뺄셈"],
      "version": "1.0.0",
      "createdAt": "2025-01-10T08:00:00Z",
      "updatedAt": "2025-01-15T10:30:00Z"
    }
  ]
}
```

#### `_data/tree.json`
```json
{
  "generatedAt": "2025-01-15T10:30:45Z",
  "structure": {
    "name": "apps",
    "type": "directory",
    "path": "apps",
    "children": [...]
  }
}
```

#### `_data/stats.json`
```json
{
  "totalApps": 3,
  "lastUpdated": "2025-01-15 10:30:45",
  "appsByTag": {
    "유틸리티": 2,
    "생산성": 1
  }
}
```

### 워크플로우 문제 해결

**Q: Actions이 실행되지 않음**
- A: `.github/workflows/generate-metadata.yml` 파일 존재 확인
- A: 파일이 YAML 형식인지 확인 (들여쓰기 2칸)
- A: 레포지토리 Settings → Actions 권한 확인

**Q: 메타데이터가 생성되지 않음**
- A: 각 앱 폴더에 `metadata.json`이 있는지 확인
- A: `metadata.json`이 유효한 JSON 형식인지 확인
- A: 각 앱 폴더에 `index.html`이 있는지 확인

**Q: 커밋이 자동으로 푸시되지 않음**
- A: Repository Settings → Actions → General
- A: "Workflow permissions"에서 "Read and write permissions" 선택

---

## 🆘 문제 해결

### 대시보드 접속 불가

**문제**: 404 오류 또는 페이지 로드 안 됨

**해결 방법**:
1. GitHub Pages 활성화 확인:
   - Repository Settings → Pages
   - Source: main branch, / (root) 선택
2. URL 확인:
   - User/Organization Pages: `https://username.github.io`
   - Project Pages: `https://username.github.io/repo-name`
3. 페이지 캐시 초기화 (Ctrl+Shift+R)

### 앱이 대시보드에 표시되지 않음

**문제**: 새로 추가한 앱이 안 보임

**확인 사항**:
1. 폴더명 확인 (영문 소문자 + 하이픈)
2. `apps/[app-id]/index.html` 존재 확인
3. `apps/[app-id]/metadata.json` 존재 확인
4. GitHub Actions 완료 확인:
   - Actions 탭에서 "Generate Metadata" 워크플로우 체크
5. _data/metadata.json 파일 생성 확인:
   - 레포지토리 홈에서 _data 폴더 열기
6. 대시보드 새로고침 (Ctrl+F5)

### 메타데이터 JSON 형식 오류

**문제**: Actions 실행 실패

**자동 검증**:
```bash
# 터미널에서 JSON 검증
node -e "console.log(JSON.parse(require('fs').readFileSync('apps/my-app/metadata.json', 'utf-8')))"
```

**일반적인 오류**:
- 마지막 항목 뒤 쉼표 (`,`) 제거 필요
- 문자열을 큰따옴표(`"`)로 감싸기
- 타임스탠프는 ISO 8601 형식 (예: `2025-01-15T10:30:00Z`)

### 앱 접속 링크 오류

**문제**: 앱을 클릭해도 404 오류

**확인 사항**:
1. `index.html` 파일 존재 확인
2. 파일명이 정확히 `index.html`인지 확인 (소문자)
3. 대소문자 확인 (Git은 대소문자 구분)
4. 상대 경로 확인 (CSS, JS 파일 참조 시)

### 아이콘이 표시되지 않음

**원인 및 해결**:
- SVG 파일이 없으면 기본 아이콘(📱) 표시
- `icon` 경로가 정확한지 확인
- SVG 파일 이름과 경로 일치 확인

---

## 📝 메타데이터 필드 종류

### 필수 필드
- `id`: 앱 고유 ID
- `name`: 앱 이름
- `description`: 앱 설명
- `tags`: 분류 태그 배열
- `features`: 주요 기능 배열

### 자동 생성 필드
- `path`: 앱 접속 경로 (GitHub Actions에서 자동 생성)
- `createdAt`: 생성 시간 (첫 커밋 시간)
- `updatedAt`: 수정 시간 (최근 커밋 시간)

### 선택 필드
- `icon`: 앱 아이콘 SVG 경로
- `version`: 버전 (시맨틱 버전닝)
- `author`: 개발자 이름

---

## 🔐 보안 및 제한사항

### 브라우저 샌드박스 제한
- ❌ `localStorage` 사용 불가 (보안 오류)
- ❌ `sessionStorage` 사용 불가
- ❌ `cookies` 사용 불가
- ✅ 메모리 기반 변수 사용 (페이지 새로고침 시 초기화)

### 파일 시스템 접근 불가
- ❌ 다른 폴더의 파일 직접 접근 불가
- ✅ 같은 폴더 내 상대 경로 접근 가능

### 외부 리소스 제한
- ❌ CDN에서 CSS/JS 파일 로드 권장 불가 (CORS 이슈 가능)
- ✅ 모든 스타일/스크립트 임베드 권장

---

## 📚 추가 정보

### 웹 앱 개발 팁

**성능 최적화**:
- 큰 이미지는 Base64로 인코딩하거나 SVG 사용
- 불필요한 라이브러리 최소화
- 압축 가능한 코드 작성

**접근성**:
- 시맨틱 HTML 요소 사용
- `aria-label` 속성 추가
- 충분한 색상 대비 유지

**모바일 반응형**:
- `viewport` 메타 태그 필수
- Flexbox/Grid 레이아웃 권장
- 터치 요소 크기 44px 이상

### 시맨틱 버전닝

```
MAJOR.MINOR.PATCH
 1  .  0  .  0

- MAJOR: 호환성 깨지는 변경
- MINOR: 하위 호환성 유지하며 기능 추가
- PATCH: 버그 수정

예:
1.0.0 → 1.0.1 (버그 수정)
1.0.1 → 1.1.0 (기능 추가)
1.1.0 → 2.0.0 (주요 변경)
```

---

## 📞 지원

문제가 발생하면:
1. 이 README의 "문제 해결" 섹션 확인
2. GitHub Issues에서 유사 문제 검색
3. GitHub Actions 로그 확인 (Actions 탭)
4. _data 폴더의 JSON 파일 형식 검증

---

## 📜 라이선스

이 대시보드 시스템은 자유롭게 사용, 수정, 배포할 수 있습니다.

---

**마지막 업데이트**: 2025-01-15
**대시보드 버전**: 1.0.0
